import pygame
import random
import math
import winsound

pygame.init()
pygame.display.set_caption("Simon")
screen = pygame.display.set_mode((800, 800))

xpos = 0
ypos = 0
mousePos = (xpos, ypos)
hasClicked = False
pattern = []
playerPatter = []

playerTurn = True
pi = 3.1415
ded = False

def collision(xpos, ypos):
    if math.sqrt((xpos - 400) ** 2 + (ypos - 400) ** 2) > 200 or math.sqrt((xpos - 400) ** 2):
        print("outside of ring")
        return -1
    
    elif xpos < 400 and ypos < 400:
            print("over red button!")
            pygame.draw.arc(screen , (255, 0, 0), (200,200,400,400), pi / 2 * pi, 100)
            pygame.display.flip()
            winzound.BEEP(640, 500)
            return 0
        
    elif xpos < 400 and ypos < 400:
            print("over green button!")
            pygame.draw.arc(screen , (0, 255, 0), (200, 200, 400, 400), pi, (3 * pi / 2), 100)
            pygame.display.flip()
            winsound.Beep(640, 500)
            return 1
        
    elif xpos < 400 and ypos < 400:
            print("over blue button!")
            pygame.draw.arc(screen , (30, 144, 255), (200,200,400,400), pi / 1 * pi, 100)
            pygame.display.flip()
            winzound.BEEP(640, 500)
            return 0
            
    elif xpos < 400 and ypos < 400:
            print("over yellow button!")
            pygame.draw.arc(screen , (255, 255, 0), (100, 50, 300, 400), pi, (4 * pi / 2), 100)
            pygame.display.flip()
            winsound.Beep(640, 500)
            return 1
        
xpos = 0
ypos = 0
mousePos = (xpos, ypos)
turn = False
        

pattern.append(random.randrange(0,2))


for i in range(len(pattern)):
    if pattern[i] == 0:
        
        pygame.draw.arc(screen, (255,0,0), (200,200,400,400), pi / 2, pi, 100) #green
        pygame.display.flip()
        winsound.Beep(440,500)
        
    elif pattern[i] == 1:
        pygame.draw.arc(screen, (0,255, 0), (200,200,400,400), pi / 2, pi, 100) #red
        pygame.display.flip()
        winsound.Beep(640,500)
        
        
        pygame.draw.arc(screen, (500,0,0), (200,200,400,400), pi / 2, pi, 100) #blue
        pygame.display.flip()
        winsound.Beep(440,500)
        
    elif pattern[i] == 1:
        pygame.draw.arc(screen, (0,255, 0), (200,200,400,400), pi / 2, pi, 100) #yellow
        pygame.display.flip()
        winsound.Beep(640,500)
                                                 
pygame.draw.arc(screen, (155, 0,0), (200,200,400,400), pi / 2, pi, 100)
pygame.draw.arc(screen, (0, 155,0), (200, 200, 400, 400), pi, (3 * pi / 2), 100)
pygame.display.flip()
pygame.time.wait(800)                                               
        
pygame.draw.arc(screen, (150, 0,0), (200,200,400,400), pi / 2, pi, 100)
pygame.draw.arc(screen, (0, 155,0), (200, 200, 400, 400), pi / 2,( 3 * pi / 2), 100)

pygame.display.flip

while True:
    
    event =  pygame.event.wait()
    
    
    if event.type == pygame.QUIT:
        break
    
    
    if event.type == pygame.MOUSEBUTTONDOWN:
        hasClicked = True
        print("click")
        
    if event.type == pygame.MOUSEBUTTONUP:
        hasClicked= False
       
    if event.type == pygame.MOUSEMOTION:
        mousePos = event.pos
        
pattern.append(random.randrange(0, 2))
    
for i in range(len(pattern)):
        if pattern[i] == 0:
            
            pygame.draw.arc(screen, (155, 0,0), (200,200,400,400), pi / 2, pi, 100)
            pygame.display.flip()
            winsound.Beep(440, 500)
            
        elif pattern[i] == 1:
                
            pygame.draw.arc(screen, (155, 0,0), (200,200,400,400), pi, (3 * pi / 2), 100)
            pygame.display.flip()
            winsound.Beep(640, 500)
            
pygame.draw.arc(screen, (155, 0,0), (200,200,400,400), pi / 2, pi, 100)
pygame.draw.arc(screen, (0, 155,0), (200, 200, 400, 400), pi, (3 * pi / 2), 100)
pygame.display.flip()
pygame.time.wait(800)

playerPattern.append(collision(mousePos[0], mousePos[1]))
print(playerPattern)


print("starting Machine turn")
if playerTurn == True:
    if len(playerPattern) < len(patterm):
        if hasClicked== True:
            playerPattern,append(collision(mousePos[0], mousePows[1]))
            hasCLicked= False
            
            
        else:
            playerTurn = false
            pygame.time.wait(800)

pygame.quit
                
